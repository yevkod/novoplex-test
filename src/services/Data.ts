import { casinoStore } from "@/store"

const Data = () => {
    const { setState, getState } = casinoStore;

    const getData = async () => {
        try {
            const response = await 
        }
    }
}